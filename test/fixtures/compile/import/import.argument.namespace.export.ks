func corge(): Number => 42

func grault(n: Number): Number => n + 42

func garply(s: String): String => s.toUpperCase()

func waldo(): String => 'miss White'

export namespace foobar {
	export {
		corge
		grault
		garply
		waldo
	}
}