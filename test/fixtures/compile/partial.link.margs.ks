class Shape {
}

let name = 'draw'
let shape = 'rectangle'
let color = 'blue'

func draw(shape, color, canvas): string {
	return `I'm drawing a \(color) \(shape).`
}

impl Shape {
	`\(name)`(canvas) for draw with shape, color
}