func foobar(c1, c2, v2) {
	var v1 = v2.substring(
		v2.lastIndexOf(
			c1
			if v2.contains(c2) {
				set v2.indexOf(c2)
			}
			else {
				set v2.length
			}
		) + 1
		if v2.contains(c2) {
			set v2.indexOf(c2, v2.lastIndexOf(c1, v2.indexOf(c2)))
		}
		else {
			set v2.length
		}
	)
}