/*
require expect: func

struct Pair(String, Number)

let pair = Pair('x', 0.1)

expect(pair.0).to.equal('x')
expect(pair.1).to.equal(0.1)

expect(pair[0]).to.equal('x')
expect(pair[1]).to.equal(0.1)

let [str, dec] = pair

expect(str).to.equal('x')
expect(dec).to.equal(0.1)
*/
/*
require expect: func

struct Point {
    x: Number
    y: Number
}

let point = Point({
    x: 0.3
    y: 0.4
})

expect(point.x).to.equal(0.3)
expect(point.y).to.equal(0.4)

let {x: my_x, y: my_y} = point

expect(my_x).to.equal(0.3)
expect(my_y).to.equal(0.4)
*/