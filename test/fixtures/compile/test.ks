/* enum Weekday {
    MONDAY
    TUESDAY
    WEDNESDAY
    THURSDAY
    FRIDAY
    SATURDAY
    SUNDAY

    isWeekend(): Boolean => this == SATURDAY | SUNDAY

    static fromString(value: String): Weekday? {
        switch value {
            'monday' => return MONDAY
        }

        return null
    }
}

func foobar(day: Weekday) {
    if day.isWeekend() {
    }
} */

/* struct Point {
    x: Number	= null
    y: Number	= null
}

const p = Point()

p.x = null */

/* import './class/class.static.field.default.ks'


const foo = new Foo('xyz')

foo.qux('') */
